 two files:
translate_file.py: The main script that handles the translation
requirements.txt: Contains the required package
To use this script:
First, install the required package:

pip install -r requirements.txt

Then run the script by providing a text file as input:

python translate_file.py your_text_file.txt